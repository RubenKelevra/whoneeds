[package]
name = "whoneeds"
version = "1.1.0"
edition = "2021"
description = "Show installed Arch packages that depend on a package, grouped by install reason"
license = "GPL-2.0-only"

[dependencies]
